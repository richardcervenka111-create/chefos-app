# ChefOS — Health log

One line per issue: date | issue | root cause | guard added. Full standard in the
2026-07-15 health-check report; rules of the road in `db/README.md` + `DEPLOY_CHECKLIST.md`.

- 2026-07-15 | No automated checks anywhere — 5 bug classes (structural breaks, dead buttons, ghost tables, missing RLS, silent no-op mutations) each shipped at least once, caught only by humans | nothing ran before code reached prod | scripts/audit_db.py + scripts/audit_app.py wired into .githooks (pre-commit/pre-push) AND CI on every push/PR AND as a gate on the production Pages deploy
- 2026-07-15 | Secrets exposure risk (historic client-side API key era) | no scanner, single-file habits | full git-history scan came back clean (only a UI placeholder matches); secret-pattern scan (Anthropic/Resend/JWT/sb_secret) now blocks commits, pushes and deploys
- 2026-07-15 | Multi-tenant regression risk (3 real RLS incidents in one week: db/53 outage, db/55, db/62 lockout) | RLS changes shipped with zero verification | audit_db.py fails any new table without kitchen_id/RLS/policies; scripts/tenant_isolation_test.sql raises on any cross-kitchen leak (run on staging after every RLS-touching migration — db/README.md rule 7)
- 2026-07-15 | Silent no-op mutations under RLS (Check List delete bug class) | Supabase update/delete return success with 0 rows affected when RLS filters everything | ratchet: unchecked delete/update call-site count (baseline 9) may only shrink; growth fails CI
- 2026-07-15 | Destructive SQL unguarded (db/18 dropped tables with no marker) | no convention existed | audit_db.py requires `-- DESTRUCTIVE: <reason>` + Richard's explicit approval; db/18 annotated retroactively
- 2026-07-15 | Docs contradicted code (docs/ROADMAP.md frozen at 2026-07-08 claiming "Phase 1 in discussion") | two sources of truth, one unmaintained | phase table synced to reality + pointer added: living status = visual data/status.html; weekly snapshot automation diffs docs vs code every Sunday
- 2026-07-15 | Monolith risk (index.html was 14,696 lines, one file = un-reviewable diffs) | historic single-file constraint | first seam cut: CSS (~1,360 lines) extracted byte-identically to app/styles.css (ON BRANCH, pending Richard's live check); auditor now covers .css too; JS extraction OPEN — needs live-browser smoke tests per extraction
- 2026-07-15 | error_logs anonymous INSERT (deliberate db/73 trade-off) re-reviewed | pre-login errors had to log themselves | db/79 DRAFT caps row sizes (staging first); volume abuse accepted as LOW risk (no anon read path, Supabase rate limits) — revisit before public trial
- 2026-07-15 | OPEN: Face ID stores a Supabase refresh token in device localStorage | no server-side WebAuthn verification backend exists (deliberate scope cut) | biometric gate + per-use token rotation mitigate; structural fix = real WebAuthn verification server-side — needs product decision, tracked in backlog
- 2026-07-15 | OPEN: no JS runtime on the build machine (no node/deno/npm) | never needed before | JS unit tests (scaling, allergens) impossible locally today; CI could run them once logic is extracted from the monolith — blocked on the JS-extraction step above
- 2026-07-15 | DB backup verification | no Supabase account access from the health-check session | RESOLVED same day: Richard confirmed the project runs on Supabase Pro (daily backups included). Residual: do one test RESTORE on staging someday — a backup never restored is a hope, not a backup
- 2026-07-15 | Privilege escalation: "update own profile" policy (db/34) let any user set their own is_admin=true | policy had no column restriction; open since db/34 | db/80 DRAFT (staging first): resets admin to richard.cervenka@icloud.com only + BEFORE UPDATE trigger blocks is_admin changes by anyone else (trigger, not column grants — the db/62 lockout lesson); app no longer self-grants admin on kitchen creation
- 2026-07-16 | Second privilege gap in the invite system: "create kitchen invite" policy (db/53) checked kitchen membership only, not admin — any team member could mint invite QR/links, not just the admin | same class as the profiles gap above, introduced when invites were built (13.7.), never revisited | db/81 (staging first): kitchen_invites INSERT/UPDATE now require is_admin; app-side self-serve "create your own kitchen" also restricted to richard.cervenka@icloud.com only
- 2026-07-16 | Production-risk function reachable by any live user: "🎲 Add sample data (test only)" button in Working Time > Hours by Month wrote random fake check-in/check-out rows into the CURRENT user's real time_entries, with no flag distinguishing synthetic rows from real ones afterward | a developer preview aid (built to eyeball chart states without waiting on real data) shipped into the same production build real pilot users log real hours in, with no admin gate and no synthetic-data marker | removed the button and the now-dead generateSampleWorkingHours() function entirely — real payroll-adjacent data should never share a table with unmarked fake rows
- 2026-07-16 | Migration backlog risk: 11 migrations (db/74 through db/84) queued unrun since 15.7., spanning two real security fixes (admin lockdown db/80, invite lockdown db/81) that are NOT protecting production until applied | fast build pace outrunning the "Richard runs migrations personally" step; nothing currently tracks migration age | no code guard for this (a process gap, not a code one) — flagged as priority #1 in the 16.7. review; consider a lightweight "days since oldest unrun migration" indicator in status.html if the backlog keeps growing
- 2026-07-16 | Auditor re-run after all 16.7. changes (18 files touched today) | routine verification | scripts/audit_db.py (37 tables) + scripts/audit_app.py both clean; unchecked-mutation ratchet holds at baseline 9, no regressions
- 2026-07-16 | Super-admin locked out of his own account within hours of running db/85+86 — app showed "Waiting for approval" as if he were a stranger | TWO stacked bugs, both mine, both invisible in the SQL editor (superuser bypasses RLS + column grants): (1) db/86 recreated the db/53 recursion anti-pattern — policies ON profiles subquerying profiles (42P17 poisons every profiles query for everyone); (2) db/85 added admin_perms/email columns but never extended db/62's column-level GRANT allow-list, so startApp()'s own profile fetch was rejected outright | db/90 hotfix (SECURITY DEFINER helpers + grant extension, applied live 16.7.); audit_db.py check #6 now HARD-FAILS any policy that subqueries its own table (verified it catches both db/53 and db/86 retroactively); standing lesson: a migration touching profiles RLS/grants must be smoke-tested through the `authenticated` role, never judged by SQL-editor success
- 2026-07-16 | db/76 (Order List per-person rows), last unrun migration from 15.7., failed on first live attempt: `operator does not exist: name[] = text[]` | the dynamic constraint-lookup compared `array_agg(pg_attribute.attname)` (type `name`) directly against a `text[]` literal — Postgres implicitly casts `name`↔`text` for scalar comparisons but not for array-to-array equality, an easy trap in any migration that introspects `pg_attribute`/`pg_class` by column name | fixed with `array_agg(attname::text ...)`; zero risk from the failed attempt since Supabase runs a pasted script as one implicit transaction — the DO block's exception rolled back before the ALTER TABLE could run, confirmed via re-diagnosis, not assumption. All 11 migrations queued since 15.7. (76, 80–90) are now confirmed live on production.
